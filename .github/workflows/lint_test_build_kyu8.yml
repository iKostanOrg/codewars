---
name: Kyu8 Build Pipeline

on:   # yamllint disable-line rule:truthy
  push:
    branches:
      - 'kyu8'

jobs:
  markdown:
    name: Markdown Lint
    uses: iKostanOrg/codewars/.github/kyu8/markdown_lint.yml@kyu8
